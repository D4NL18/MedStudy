# Phase 35: Refatoração Frontend - Separação de Arquivos (HTML, SCSS, TS)

**Wave 1: Script de Extração**
- Identificar todos os componentes em rontend/src/app/**/*.ts com 	emplate: ou styles: inline.
- Para cada componente encontrado:
  - Extrair o conteúdo do 	emplate para [nome].component.html.
  - Extrair o conteúdo de styles para [nome].component.scss.
  - Atualizar a declaração do @Component no arquivo .ts substituindo 	emplate por 	emplateUrl: './[nome].component.html' e styles por styleUrls: ['./[nome].component.scss'].

**Wave 2: Atualização de Testes (Unit Tests)**
- Modificar os arquivos [nome].component.spec.ts correspondentes para garantir que após o TestBed.configureTestingModule, seja chamado o wait TestBed.compileComponents(); (visto que os templates agora são carregados assincronamente dos arquivos externos).

**Wave 3: Verificação**
- Rodar o build do frontend (
g build) para garantir a compilação do TypeScript e do SCSS sem erros.
- Rodar os testes do Angular (
g test --watch=false ou similar se disponível) para garantir que a refatoração não quebrou os testes básicos.
