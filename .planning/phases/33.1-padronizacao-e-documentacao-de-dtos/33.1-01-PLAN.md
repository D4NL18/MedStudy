---
wave: 1
depends_on: []
files_modified:
  - backend/src/main/java/com/medstudy/backend/core/constants/ValidationMessages.java
  - backend/src/main/java/com/medstudy/backend/dto/**/*.java
autonomous: true
requirements_addressed: [BKND-03, DOC-03]
---

# Plan 01: Padronização de DTOs e Swagger (Backend)

## Objective
Converter DTOs para classes Lombok, centralizar mensagens de erro no core e expandir documentação do Swagger com exemplos e descrições obrigatórias.

## Tasks

### 1. Criar Arquivo Central de Mensagens
**<read_first>**
- backend/src/main/java/com/medstudy/backend/core/config/DataInitializer.java (to see package structure)
**</read_first>**
**<action>**
Create `ValidationMessages.java` inside `backend/src/main/java/com/medstudy/backend/core/constants/`.
It should be a `public final class` or `interface` with `public static final String` constants for all common validation messages (e.g., `FIELD_REQUIRED`, `INVALID_EMAIL`, etc.).
**</action>**
**<acceptance_criteria>**
- File `backend/src/main/java/com/medstudy/backend/core/constants/ValidationMessages.java` exists and contains string constants.
**</acceptance_criteria>**

### 2. Refatorar DTOs para Classes Lombok e Integrar Mensagens
**<read_first>**
- backend/src/main/java/com/medstudy/backend/dto/**/*.java
**</read_first>**
**<action>**
Convert any `record` to `class` in the DTO layer.
Add Lombok annotations: `@Data`, `@Builder`, `@NoArgsConstructor`, `@AllArgsConstructor`.
Replace hardcoded validation messages in annotations (like `@NotBlank`, `@NotNull`, `@Size`) with constants imported from `ValidationMessages.java`.
**</action>**
**<acceptance_criteria>**
- No `record` keywords remain in `backend/src/main/java/com/medstudy/backend/dto`.
- All DTO classes use `@Data`.
- Hardcoded messages in validation annotations are replaced with `ValidationMessages.CONSTANT_NAME`.
**</acceptance_criteria>**

### 3. Expandir Documentação Swagger nos DTOs
**<read_first>**
- backend/src/main/java/com/medstudy/backend/dto/**/*.java
**</read_first>**
**<action>**
Add `@Schema(description = "...", example = "...")` to EVERY field in EVERY DTO class.
**CRITICAL REQUIREMENT:** All Swagger descriptions and comments MUST be written in English.
**</action>**
**<acceptance_criteria>**
- Every field in DTO classes has a `@Schema` annotation with both `description` and `example` attributes.
**</acceptance_criteria>**
