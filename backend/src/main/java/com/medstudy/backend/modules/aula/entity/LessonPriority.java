package com.medstudy.backend.modules.aula.entity;

/**
 * Enumeration representing the priority levels for a Lesson.
 */
public enum LessonPriority {
    /** Diamond priority (Highest). */
    DIAMANTE,

    /** High priority. */
    ALTA,

    /** Medium priority. */
    MEDIA,

    /** Low priority. */
    BAIXA
}
